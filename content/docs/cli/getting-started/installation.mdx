---
title: Installing the CLI
description: Install the LogisBase CLI globally via npm so the `flb` command is available system-wide.
---

# Installing the CLI

The LogisBase CLI is distributed on npm as `@logisbase/cli`. Install it globally so the `flb` command is on your PATH.

## With npm

```bash
npm install -g @logisbase/cli
```

## With pnpm

```bash
pnpm add -g @logisbase/cli
```

## With Yarn

```bash
yarn global add @logisbase/cli
```

## Verify

```bash
flb --version
```

You should see something like:

```
@logisbase/cli 0.0.6
```

## Get Help

Every command supports `--help`:

```bash
flb --help
flb scaffold --help
flb install --help
```

The top-level `flb --help` lists all available commands.

## Updating

To update an existing install:

```bash
npm install -g @logisbase/cli@latest
# or
pnpm add -g @logisbase/cli@latest
```

## Uninstalling

```bash
npm uninstall -g @logisbase/cli
# or
pnpm remove -g @logisbase/cli
```

## Next Steps

- [Install LogisBase](/docs/cli/installation/overview) — install a self-hosted LogisBase instance with the wizard
- [Search Extensions](/docs/cli/extensions/search) — discover extensions in the registry
- [Register an Account](/docs/cli/account/register) — create a registry account for publishing
