---
title: Extensions
description: How to build, publish, and contribute extensions for the LogisBase Marketplace.
sidebarTitle: Extensions
---

# Extension contributions

The biggest single way to contribute to LogisBase is building an **extension** — a packaged module that other operators can install from the marketplace.

Extensions can:

- Add a new module to the Console (custom screens, settings, dashboards)
- Integrate with a third-party service (mapping, payments, telematics, ERPs)
- Add new order types, workflows, or business logic
- Provide industry-specific functionality (medical delivery, container freight, food trucks)

Extensions are full-stack — a Laravel backend package and an Ember.js frontend engine — registered through the LogisBase extension system.

## Where to start

This page is a contribution-flow overview. The full developer guide for building extensions lives in its own section:

<Cards>
  <Card
    title="Extension Development"
    href="/docs/extension-development"
    description="Complete guide to scaffolding, building, testing, and publishing extensions."
  />
</Cards>

Read that section first if you've never built an extension before. The notes below cover **contributing** — getting your extension into the wider LogisBase ecosystem.

## Two kinds of extension contributions

### 1. Publish your own extension

You build an extension to solve your own operational need. You publish it to the marketplace. Other operators install it.

You retain ownership and control:

- You set the price (or make it free)
- You handle support
- You decide when to update
- You keep all revenue (minus marketplace fees)

The marketplace handles distribution, licensing, and payment infrastructure on your behalf.

See [Building Extensions](/docs/extension-development) for the developer flow, and the [Marketplace developer dashboard](https://console.logisbase.com) for publishing.

### 2. Contribute to a LogisBase-published extension

The first-party extensions (FleetOps, Storefront, Pallet, Ledger, IAM) are source-accessible and accept community contributions. You can:

- Fix bugs in the extension
- Add features that fit the extension's scope
- Improve the docs for the extension
- Translate the extension into your language

These follow the same flow as core code contributions. See [Code →](/docs/contributing/code) for the PR conventions, and the relevant extension's repo:

- [`logisbase/fleetops`](https://github.com/logisbase/fleetops)
- [`logisbase/storefront`](https://github.com/logisbase/storefront)
- [`logisbase/pallet`](https://github.com/logisbase/pallet)
- [`logisbase/ledger`](https://github.com/logisbase/ledger)
- [`logisbase/iam-engine`](https://github.com/logisbase/iam-engine)

## What makes a good extension

Whether you're publishing your own or contributing to a LogisBase-published one:

- **Solves a real problem** — built for an operator's actual workflow, not a hypothetical one
- **Scoped narrowly** — does one thing well; doesn't try to replace a separate extension
- **Stable** — doesn't crash on edge cases, handles offline/unauthenticated states gracefully
- **Documented** — has clear setup instructions, screenshots, and an example use case
- **Localised** — supports multiple locales (or at least leaves the door open for translations)
- **Tested** — has automated tests for critical paths

## Where to ask questions

- **Building your first extension?** Start in `#extension-development` on [Discord](/docs/community/discord)
- **Publishing flow questions?** [`logisbase/registry-bridge`](https://github.com/logisbase/registry-bridge) issues
- **Specific to a first-party extension?** Open an issue on that extension's repo

## What you'll need

Before you start building anything publishable:

- A **developer account** on the LogisBase Marketplace ([sign up](https://console.logisbase.com/onboard))
- The **LogisBase CLI** installed: `npm install -g @logisbase/cli`
- A **local LogisBase instance** for development (see [System Setup](/docs/platform/system-setup))
- **Stripe Connect** if you plan to charge for the extension

Continue to [Extension Development](/docs/extension-development) for the full developer guide.
